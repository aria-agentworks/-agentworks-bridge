#!/usr/bin/env python3
import urllib.request
import urllib.parse
import ssl
import json

print('=== Shadow Architect: Forced Handshake Test ===')
url = 'https://results.manabadi.co.in/ts-10th-class-results-2026-regular-123456' 
headers = {
    'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64)',
    'Content-Type': 'application/x-www-form-urlencoded',
    'Referer': 'https://www.manabadi.co.in/results/2026/TS/SSC/',
    'Origin': 'https://www.manabadi.co.in'
}
data = urllib.parse.urlencode({
    'htno': '0512345678', 
    'dob': '15-05-1989',
    'class': 'SSC',
    'exam_year': '2005',
    'submit': 'Get Result'
}).encode('utf-8')

# Create unverified context to bypass SSL mismatch
context = ssl._create_unverified_context()

try:
    print(f'Sending FORCED POST to {url}...')
    req = urllib.request.Request(url, data=data, headers=headers)
    with urllib.request.urlopen(req, context=context, timeout=30) as response:
        status = response.getcode()
        content = response.read().decode('utf-8', errors='ignore')
        print(f'=== Handshake Status: {status} ===')
        print(f'Response Size: {len(content)} bytes')
        if 'Result Not Found' in content or 'Invalid' in content or 'Marks' in content:
            print('Handshake established: Legacy DB response received.')
        else:
            print('Unexpected response structure.')
    print('=== STATUS 200 ===')
except Exception as e:
    print(f'Error: {e}')
    print('=== STATUS 503 ===')