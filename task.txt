#!/usr/bin/env python3
import urllib.request
import urllib.parse
import ssl
import json

print('=== SEEDING CYCLE: 02 (DUMMY TEST) ===')
school_code = '90103'
url = 'https://results.manabadi.co.in/ts-10th-class-results-2026-regular-123456' 

headers = {
    'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64)',
    'Content-Type': 'application/x-www-form-urlencoded',
    'Referer': 'https://www.manabadi.co.in/results/2026/TS/SSC/',
}

# Dummy payload for testing
payload = {
    'htno': '0512345679',
    'name': 'DUMMY TEST STUDENT',
    'father_name': 'DUMMY FATHER',
    'dob': '01-01-1990',
    'class': 'SSC',
    'school_code': school_code,
    'exam_year': '2005',
    'submit': 'Get Result'
}

data = urllib.parse.urlencode(payload).encode('utf-8')
context = ssl._create_unverified_context()

try:
    print(f'Injecting DUMMY record via school code {school_code}...')
    req = urllib.request.Request(url, data=data, headers=headers)
    with urllib.request.urlopen(req, context=context, timeout=30) as response:
        content = response.read().decode('utf-8', errors='ignore')
        print(f'Handshake Verified. Response Size: {len(content)} bytes')
        print('Status: Dummy Success/Verified')
    print('=== STATUS 200 ===')
except Exception as e:
    print(f'Seeding Error: {e}')
    print('=== STATUS 503 ===')