#!/bin/bash
echo "=== Maya Growth: Mission 01 (Remote Browser) ==="

# 1. Install dependencies in the current env
pip3 install browser-harness cdp-use --break-system-packages --quiet

# 2. Run the mission via python3 -c
python3 -c "
import time
import json
from browser_harness.run import set_cdp_url, goto_url, page_info

try:
    set_cdp_url('http://4.213.114.95:9222')
    print('Connecting to Remote Browser...')
    goto_url('https://www.instagram.com/natural_beauty_shoutout/')
    time.sleep(5)
    info = page_info()
    print('=== page_info() ===')
    print(json.dumps(info, indent=4))
    if info.get('status') == 200:
        print('=== STATUS 200 ===')
        print('Mission Success!')
except Exception as e:
    print(f'Error: {e}')
"

echo "=== Mission Test Complete ==="