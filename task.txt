pip install phonenumbers requests && python3 -c "import phonenumbers; from phonenumbers import geocoder, carrier, timezone, PhoneNumberFormat; from phonenumbers.phonenumberutil import number_type; num = phonenumbers.parse('+919849257983'); print(f'Location: {geocoder.description_for_number(num, \"en\")}'); print(f'Carrier: {carrier.name_for_number(num, \"en\")}'); print(f'Timezone: {timezone.time_zones_for_number(num)}'); print(f'Type: {number_type(num)}'); print(f'E164: {phonenumbers.format_number(num, PhoneNumberFormat.E164)}'); print(f'Is Valid: {phonenumbers.is_valid_number(num)}')"